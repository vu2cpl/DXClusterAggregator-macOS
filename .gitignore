.build/
.swiftpm/
*.xcodeproj/xcuserdata/
DerivedData/
.DS_Store
*.pyc
__pycache__/
AppIcon.iconset/

# Built app bundle and release archives are NOT tracked.
# The repo is the source of truth; releases are built + notarised fresh
# per version and attached to GitHub Releases (see HANDOVER.md).
DXClusterAggregator.app/
DXClusterAggregator-*.zip

# Local Claude harness state (worktrees, session data) — never tracked.
.claude/

# Original Windows reference binary — kept locally, not part of the macOS app.
FT8ClusterAggregator.exe
